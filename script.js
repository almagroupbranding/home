# The Alma Group Website

A fast, GitHub-ready static website for **The Alma Group**, a hospitality brand transformation and growth consultancy for pubs, restaurants, bars, cafés, inns and hotels.

## Folder structure

```text
the-alma-group-website/
├── index.html
├── style.css
├── script.js
├── README.md
└── assets/
    ├── favicon.svg
    ├── logo-mark.svg
    └── og-card.svg
```

## What is included

- Mobile-first responsive layout
- Warm cream, burgundy and charcoal brand style
- SEO title, description, Open Graph image and structured data
- Clear calls to action for venue snapshots
- Services, packages, pricing ranges and process sections
- Interactive quick diagnostic checklist
- Static contact form that opens the visitor's email app
- No external dependencies, so it is fast and simple to host on GitHub Pages

## Before launch

Edit these placeholders:

1. In `index.html`, replace:
   - `https://YOUR-GITHUB-USERNAME.github.io/the-alma-group/`
   - `hello@thealmagroup.co.uk` if you want to use a different email
   - "UK hospitality venues" if you want a narrower service area
2. In `script.js`, update the `mailto:` email if needed.
3. Add real client proof, photos or case studies when available.

## Deploy on GitHub Pages

1. Create a new GitHub repository, for example `the-alma-group`.
2. Upload all files from this folder into the repository root.
3. Go to **Settings → Pages**.
4. Under **Build and deployment**, choose **Deploy from a branch**.
5. Select the `main` branch and `/root`.
6. Save. GitHub will publish the site after a short build.

Your site will usually be available at:

```text
https://YOUR-GITHUB-USERNAME.github.io/the-alma-group/
```

## Contact form note

GitHub Pages does not process form submissions by itself. This website uses a `mailto:` form so enquiries open in the visitor's email app. For more reliable lead capture, connect the form to Formspree, Netlify Forms, Basin or your CRM later.
